function animal(obj){
  // use concatination to join peices of a string with peices of an object
  return `This ${obj.color} ${obj.name} has ${obj.legs} legs.`;
}

// arrow function format
// const animal = (obj) => `This ${obj.color} ${obj.name} has ${obj.legs} legs.`;